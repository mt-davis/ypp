<%= form_for(@rsource) do |f| %>
  <% if @rsource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rsource.errors.count, "error") %> prohibited this rsource from being saved:</h2>

      <ul>
      <% @rsource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
