<%= form_for([@client, @cnote]) do |f| %>
  <% if @cnote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cnote.errors.count, "error") %> prohibited this cnote from being saved:</h2>

      <ul>
      <% @cnote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
