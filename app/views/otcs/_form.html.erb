<%= form_for(@otc) do |f| %>
  <% if @otc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@otc.errors.count, "error") %> prohibited this otc from being saved:</h2>

      <ul>
      <% @otc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
