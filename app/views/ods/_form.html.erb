<%= form_for(@od) do |f| %>
  <% if @od.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@od.errors.count, "error") %> prohibited this od from being saved:</h2>

      <ul>
      <% @od.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
