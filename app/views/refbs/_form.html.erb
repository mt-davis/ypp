<%= form_for(@refb) do |f| %>
  <% if @refb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@refb.errors.count, "error") %> prohibited this refb from being saved:</h2>

      <ul>
      <% @refb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
