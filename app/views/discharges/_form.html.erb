<%= form_for(@discharge) do |f| %>
  <% if @discharge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discharge.errors.count, "error") %> prohibited this discharge from being saved:</h2>

      <ul>
      <% @discharge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reason %><br>
    <%= f.text_field :reason %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
