<%= form_for(@mj) do |f| %>
  <% if @mj.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mj.errors.count, "error") %> prohibited this mj from being saved:</h2>

      <ul>
      <% @mj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
